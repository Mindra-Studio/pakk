{
  "name": "@pakk/action",
  "version": "1.0.0",
  "description": "GitHub Action for PAKK cache - 2x smaller than actions/cache",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "build:post": "ncc build src/post.ts -o dist/post --source-map",
    "dev": "ncc build src/index.ts -o dist --source-map --watch"
  },
  "dependencies": {
    "@actions/cache": "^4.0.0",
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/glob": "^0.4.0",
    "@actions/io": "^1.1.3",
    "zstd-napi": "^0.0.12"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.6.0"
  }
}
